import React from 'react';
import PageHeader from '../components/common/PageHeader';

const Carrito = () => {
  return (
    <section>
        <PageHeader />
    </section>
  );
};

export default Carrito;